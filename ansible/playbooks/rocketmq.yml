---
# Usage:
# standalone (1 host: 1 namesrv + 1 broker)
#   uv run ansible-playbook playbooks/rocketmq.yml --limit rocketmq_standalone_kvm
# 2m-2s-sync (2 hosts: each runs namesrv + 1 master + 1 slave, cross-paired)
#   uv run ansible-playbook playbooks/rocketmq.yml --limit rocketmq_2m2s_kvm -e "rocketmq_mode=2m-2s-sync"
# failover / DLedger (>=3 hosts for Raft quorum: each runs namesrv + 1 DLedger broker)
#   uv run ansible-playbook playbooks/rocketmq.yml --limit rocketmq_failover_kvm -e "rocketmq_mode=failover"

- name: Deploy RocketMQ
  hosts: all
  become: true
  serial: 1
  roles:
    - rocketmq
