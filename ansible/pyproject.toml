[project]
name = "automation-ansible"
version = "0.1.0"
description = "Ansible control environment for the automation repository"
requires-python = ">=3.13,<3.14"
dependencies = [
  "ansible-core",
  "ansible-lint",
  "boto3>=1.35.0,<2.0.0",
  "botocore>=1.35.0,<2.0.0",
]
