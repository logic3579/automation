# KVM/bare-metal steady-state: connect as devops via key after kvm-init.yml has
# run. During initialization, playbooks/kvm-init.yml overrides these with
# root@22 + password in its vars block.
ansible_user: devops
ansible_port: 2233
ansible_ssh_private_key_file: keys/devops_key
