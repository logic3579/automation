---
# defaults file for kafka
# User-configurable variables

# Installation settings
kafka_root_path: /opt/kafka
kafka_version: "4.2.0"
kafka_scala_version: "2.13"
kafka_pkg_url: "https://downloads.apache.org/kafka/{{ kafka_version }}/kafka_{{ kafka_scala_version }}-{{ kafka_version }}.tgz"

# Cluster mode: "yes" enables 3-replica topic defaults (requires >=3 brokers in
# the play batch); "no" uses replication factor 1 for standalone deployments.
kafka_cluster_enabled: "no"

# JVM settings
jvm_memory_opts: "-Xms2G -Xmx2G"
