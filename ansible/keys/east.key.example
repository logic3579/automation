# This is a placeholder. Generate a real key pair with:
#   ssh-keygen -t ed25519 -C 'ansible@east' -f ansible/keys/east.key -N ""
#
# Then copy the public key to target hosts:
#   ssh-copy-id -i ansible/keys/east.pub user@host
